# LLM 추론 성능 평가 시스템 - 필수 의존성
# 안정적인 버전 범위로 관리하여 호환성 문제 방지

# ============================================================================
# 핵심 데이터베이스 연결
# ============================================================================
oracledb>=1.4.0,<2.0.0          # Oracle Database 연결 (Thin/Thick 모드 지원)

# ============================================================================
# HTTP 및 웹 관련
# ============================================================================
requests>=2.31.0,<3.0.0         # HTTP 요청 처리 (외부 데이터셋 로드용)

# ============================================================================
# 시스템 모니터링 및 성능
# ============================================================================
psutil>=5.9.0,<6.0.0            # 시스템 리소스 모니터링 (메모리, CPU 등)

# ============================================================================
# 선택적 의존성 (고급 기능용)
# ============================================================================
# 다음 패키지들은 선택적으로 설치 가능합니다:
# pip install -r requirements-optional.txt

# 데이터 분석 및 처리
# pandas>=2.0.0,<3.0.0          # 데이터 분석 및 처리
# numpy>=1.24.0,<2.0.0          # 수치 계산

# 외부 데이터셋 지원
# datasets>=2.14.0,<3.0.0       # Hugging Face datasets 라이브러리
# transformers>=4.30.0,<5.0.0   # Hugging Face transformers

# 시각화
# matplotlib>=3.6.0,<4.0.0      # 기본 플롯팅
# seaborn>=0.12.0,<1.0.0        # 통계 시각화
# plotly>=5.15.0,<6.0.0         # 인터랙티브 시각화

# 개발 도구 (개발환경에서만 필요)
# pytest>=7.0.0,<8.0.0          # 테스트 프레임워크
# pytest-cov>=4.0.0,<5.0.0      # 테스트 커버리지
# black>=23.0.0,<24.0.0         # 코드 포매터
# flake8>=6.0.0,<7.0.0          # 린터

# ============================================================================
# 버전 호환성 정보
# ============================================================================
# Python 버전: 3.8+
# Oracle Database: 12.1+ (Thin 모드), 9.2+ (Thick 모드)
#
# 설치 명령어:
# pip install -r requirements.txt
#
# 전체 기능 설치:
# pip install -r requirements.txt -r requirements-optional.txt